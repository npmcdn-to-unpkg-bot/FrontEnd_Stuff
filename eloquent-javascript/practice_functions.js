'use strict';

// Create a function that return a min value

function min(a, b) {
    if(a < b) {
        return a;
    } else {
        return b;
    }
};

console.log(min(0, 3));

console.log(min(4, 2));
